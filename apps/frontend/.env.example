# В dev лучше оставить пустым — браузер шлёт /api на Vite, а тот проксирует на PORT из apps/backend/.env (не только 3001).
# Если задаёте VITE_API_URL, укажите тот же порт, что и у backend — иначе отчёт «не считается» (сеть FETCH_ERROR).
# VITE_API_URL=http://localhost:8787
VITE_APP_NAME=Synastry
